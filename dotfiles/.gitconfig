[alias]
	uncommit = reset --soft HEAD^
[user]
	email = nope-mcnopeface@asdf.org
	name = Cédric HT
[push]
	default = simple
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
