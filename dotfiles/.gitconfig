[alias]
	uncommit = reset --soft HEAD^
[user]
	email = altaris@users.noreply.github.com
	name = Cédric HT
[push]
	default = simple
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[pull]
	rebase = false
[init]
	defaultBranch = master
